// import Match from './Match';

export default (function teste () {
  return '<h1>Teste de ambiente (build)</h1>'
})();

// No Main, eu vou usar os componentes para desenvolver a interação com os elementos do DOM
// Acho que as classes entram como Models ou Controles, não como componentes. Os componentes seriam os elementos renderizados no DOM.